rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /games/{gameId} {
      // Anyone can read any game document
      allow read: if true;
      // Writes must go through Cloud Functions
      allow write: if false; 
    }
  }
}
